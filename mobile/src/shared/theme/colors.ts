/**
 * Semantic color token definitions.
 * Hex/rgba values live only in lightTheme.ts and darkTheme.ts.
 */
export interface ColorTokens {
  primary: string;
  onPrimary: string;
  primaryMuted: string;
  accent: string;
  accentMuted: string;
  surfaceTint: string;
  background: string;
  surface: string;
  surfaceElevated: string;
  textPrimary: string;
  textSecondary: string;
  textTertiary: string;
  textInverse: string;
  border: string;
  borderFocused: string;
  error: string;
  onError: string;
  errorMuted: string;
  success: string;
  onSuccess: string;
  successMuted: string;
  warning: string;
  onWarning: string;
  warningMuted: string;
  overlay: string;
  shadow: string;
  skeleton: string;
  skeletonHighlight: string;
}
