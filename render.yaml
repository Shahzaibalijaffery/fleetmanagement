services:
  - type: web
    name: fleetlink-api
    runtime: node
    rootDir: server
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: NOTIFICATION_TIMEZONE
        value: Asia/Karachi
